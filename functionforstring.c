#include "main.h"
/**
 * _strlen - entry point
 * @s : integer value
 * Description: Write a function that returns the length of a string.
 * Return: return n
 */

int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != '\0'; c++)
	{
	}
	return (c);
}
